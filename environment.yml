# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: metagenomics-profiling-1.0
channels:
 - defaults
 - conda-forge
 - bioconda
dependencies:
 - metaphlan2=2.7.7
 - fastp=0.20.0
 - bwa=0.7.17
 - multiqc=1.7
 - samtools=1.9
 - hclust2=0.99
 - graphlan=1.1.2
